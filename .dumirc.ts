import { defineConfig } from 'dumi';

export default defineConfig({
  themeConfig: {
    logo: false,
    name: '',
    footer: '前端文档库',
    socialLinks: {
      github: 'https://github.com/liuyuqin1991'
    }
  },
});
